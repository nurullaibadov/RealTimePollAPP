namespace RealTimePoll.Domain.Enums;

public enum PollStatus
{
    Draft = 0,
    Active = 1,
    Closed = 2,
    Archived = 3
}

public enum UserRole
{
    User = 0,
    Admin = 1,
    SuperAdmin = 2
}
